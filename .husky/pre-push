remote="$1"
url="$2"

while read local_ref local_oid remote_ref remote_oid; do
  case "$remote_ref" in
    refs/heads/main)
      echo "❌ main 브랜치로 직접 push 금지. PR로 머지하세요."
      exit 1
      ;;
  esac
done

# Phase 2 에서 'npm test' 추가 예정
